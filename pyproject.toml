[project]
name = "sql-rl"
version = "0.1.0"
description = "Lightweight SQL debugging RLVR project built around BIRD-Critic style SQLite rewards."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

