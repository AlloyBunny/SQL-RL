# SQL-RL 数据文档

## 1. 结论

本项目主线使用 BIRD-Critic/SIX-GYM 的 SQL debugging 数据，而不是原始 BIRD-SQL Text-to-SQL 数据。

推荐用法：

- 训练和自建验证：`birdsql/six-gym-sqlite`。
- 外部评测和案例展示：`birdsql/bird-critic-1.0-sqlite`。
- 暂不使用：BIRD 官网 `train.zip` / `dev.zip`，因为它们是 Text-to-SQL 数据，体积大且任务定位不同。

`six-gym-sqlite` 可以直接跑训练/验证闭环，因为它同时包含：

- SQL debugging 样本：`query`, `issue_sql`, `sol_sql`, `test_cases`。
- SQLite 数据库模板：`database/{db_id}/{db_id}_template.sqlite`。

`bird-critic-1.0-sqlite` public 版本包含 500 条 benchmark 样本和数据库文件，但公开字段不含 `sol_sql/test_cases`。因此 public 版本不能直接计算官方自动评测分数；如果要正式自动评测，需要拿到 full data，或者只做执行成功率和案例分析。

## 2. 数据集关系

### 2.1 birdsql/six-gym-sqlite

链接：[birdsql/six-gym-sqlite](https://huggingface.co/datasets/birdsql/six-gym-sqlite)

定位：

- BIRD-Critic-SQLite 任务体系下的训练/Gym 数据。
- 约 5,000 条 SQL debugging/repair 样本。
- 适合 SFT、GRPO/RLVR、reward ablation、自建验证集。

实测 `train.jsonl`：

```text
train.jsonl: 13,393,232 bytes
rows: 5,000
categories:
  Query:           3,094
  Management:      1,087
  Personalization:   819
with preprocess_sql: 1,180
with clean_up_sql:    718
```

`train.jsonl` 覆盖 13 个 `db_id`：

```text
books, book_publishing_company, olympics, movie_3, netflix,
lego, chinook, employees, address, airline,
public_review_platform, car_retails, hockey
```

HF API 查询到的仓库大小：

```text
602,603,157 bytes ~= 602.6 MB
```

### 2.2 birdsql/bird-critic-1.0-sqlite

链接：[birdsql/bird-critic-1.0-sqlite](https://huggingface.co/datasets/birdsql/bird-critic-1.0-sqlite)

定位：

- BIRD-Critic-SQLite public benchmark。
- 500 条 SQLite user issue 样本。
- public 版本为防止 leakage，不公开 `sol_sql/test_cases`。
- 可以作为外部 benchmark 的输入集，但 public 版本不能直接做完整自动评分。

HF API 查询到的仓库大小：

```text
1,793,078,960 bytes ~= 1.79 GB
```

### 2.3 BIRD 官网 train/dev

链接：[BIRD-SQL 官网](https://bird-bench.github.io/)

定位：

- 原始 BIRD-SQL Text-to-SQL benchmark。
- 任务是 `question + schema/evidence -> SQL`。
- 不含 `issue_sql`，不是本项目主线的 SQL debugging 数据。

HTTP header 查询到的压缩包大小：

```text
train.zip: 8,919,543,554 bytes ~= 8.92 GB
dev.zip:     346,207,293 bytes ~= 346 MB
```

因此第一版个人项目没有必要下载 BIRD 官网 train/dev。除非后续要做 Text-to-SQL 对照实验，否则会增加数据和工程成本。

## 3. 文件结构

### 3.1 six-gym-sqlite

下载后应类似：

```text
six-gym-sqlite/
  README.md
  train.jsonl
  database/
    address/
      address_template.sqlite
    airline/
      airline_template.sqlite
    book_publishing_company/
      book_publishing_company_template.sqlite
    books/
      books_template.sqlite
    car_retails/
      car_retails_template.sqlite
    chinook/
      chinook_template.sqlite
    employees/
      employees_template.sqlite
    hockey/
      hockey_template.sqlite
    lego/
      lego_template.sqlite
    movie_3/
      movie_3_template.sqlite
    netflix/
      netflix_template.sqlite
    olympics/
      olympics_template.sqlite
    public_review_platform/
      public_review_platform_template.sqlite
```

样本中的 `db_id` 对应数据库路径：

```text
database/{db_id}/{db_id}_template.sqlite
```

例如：

```json
{"db_id": "lego"}
```

对应：

```text
database/lego/lego_template.sqlite
```

### 3.2 bird-critic-1.0-sqlite

下载后应类似：

```text
bird-critic-1.0-sqlite/
  README.md
  sqlite-00000-of-00001.jsonl
  database/
    california_schools/
      california_schools_template.sqlite
    codebase_community/
      codebase_community_template.sqlite
    european_football_2/
      european_football_2_template.sqlite
    financial/
      financial_template.sqlite
    formula_1/
      formula_1_template.sqlite
    ...
```

public 版本的 JSONL 主要用于推理输入，不足以直接自动打分。

## 4. 下载方式

推荐使用 Hugging Face CLI：

```bash
huggingface-cli download birdsql/six-gym-sqlite \
  --repo-type dataset \
  --local-dir data/raw/six-gym-sqlite
```

可选外部 benchmark：

```bash
huggingface-cli download birdsql/bird-critic-1.0-sqlite \
  --repo-type dataset \
  --local-dir data/raw/bird-critic-1.0-sqlite
```

如果 Hugging Face 直连不稳定，可以使用 hf-mirror 下载同名文件。

## 5. six-gym-sqlite 字段说明

`train.jsonl` 每行是一个 SQL debugging 样本。核心字段如下：

```json
{
  "instance_id": "TRAIN_1",
  "db_id": "employees",
  "category": "Query",
  "query": "user issue rewritten in BIRD environment",
  "issue_sql": ["buggy SQL"],
  "sol_sql": ["reference repaired SQL"],
  "test_cases": ["python test_case code"],
  "preprocess_sql": ["SQL statements executed before prediction/evaluation"],
  "clean_up_sql": ["SQL statements used to clean up temporary state"],
  "dialect": "SQLite",
  "version": "3"
}
```

字段含义：

- `instance_id`：样本 ID。
- `db_id`：数据库 ID，用于定位 SQLite 文件。
- `category`：任务类型，常见为 `Query`、`Management`、`Personalization`。
- `query`：用户问题描述。
- `issue_sql`：用户写错的 SQL。
- `sol_sql`：参考修复 SQL。
- `test_cases`：Python 判题函数。
- `preprocess_sql`：评测前要执行的 SQL，用于创建临时表、插入测试数据或调整数据库状态。
- `clean_up_sql`：评测后清理 SQL。

## 6. 真实样例

下面样例均来自 `birdsql/six-gym-sqlite/train.jsonl`。为了避免数据文档变成原始 JSONL 的复制品，特别长的英文 `query` 只写需求摘要；`instance_id`、`db_id`、`category`、`issue_sql`、`sol_sql`、`preprocess_sql` 和关键 `test_cases` 逻辑保持真实。

### 6.1 TRAIN_1：Query，带 preprocess_sql 的安全类型转换

样本定位：

```text
instance_id: TRAIN_1
db_id: employees
category: Query
database: database/employees/employees_template.sqlite
```

模型输入来自三部分：

```text
query 摘要:
  用户在 employee 数据库里处理 id 列，id 里混有数字和非数字字符串。
  目标是在 SQLite 中把可转换的值转成整数，不可转换的值给默认值。

preprocess_sql:
  CREATE TEMP TABLE employee_test (id TEXT, first_name TEXT);
  INSERT INTO employee_test VALUES ('123', 'John'), ('ABC123', 'Jane'), ('456', 'Bob');

issue_sql:
  SELECT id, CAST(id AS INTEGER) FROM employee_test
```

参考输出：

```sql
SELECT id,
       CASE
         WHEN id GLOB '[0-9]*' AND id NOT GLOB '*[^0-9]*'
         THEN CAST(id AS INTEGER)
         ELSE -1
       END AS numeric_value
FROM employee_test
```

判题逻辑：

```python
pred_sqls = remove_distinct(pred_sqls)
sol_sqls = remove_distinct(sol_sqls)
result = ex_base(pred_sqls, sol_sqls, db_path, conn, conditions)
assert result == 1
```

这个例子说明 `preprocess_sql` 不是模型要输出的内容，而是评测前用于构造临时状态。模型只需要输出修复后的 SQL。最终不是比较 SQL 字符串，而是比较模型 SQL 和 `sol_sql` 的执行结果。

### 6.2 TRAIN_5：Query，修复布尔逻辑

样本定位：

```text
instance_id: TRAIN_5
db_id: lego
category: Query
database: database/lego/lego_template.sqlite
preprocess_sql: []
clean_up_sql: []
```

模型输入：

```text
query 摘要:
  用户想在 lego_sets 表里找满足 year=1970、theme_id=414、num_parts=471
  三个条件中至少两个条件的记录。

issue_sql:
  SELECT * FROM lego_sets
  WHERE year = 1970 OR theme_id = 414 OR num_parts = 471
```

参考输出：

```sql
SELECT *
FROM lego_sets
WHERE (CASE WHEN year = 1970 THEN 1 ELSE 0 END)
    + (CASE WHEN theme_id = 414 THEN 1 ELSE 0 END)
    + (CASE WHEN num_parts = 471 THEN 1 ELSE 0 END) >= 2
```

判题逻辑：

```python
pred_sqls = remove_distinct(pred_sqls)
sol_sqls = remove_distinct(sol_sqls)
result = ex_base(pred_sqls, sol_sqls, db_path, conn, conditions)
assert result == 1
```

这个例子是典型 SQL repair：`issue_sql` 的 `OR` 会返回只满足一个条件的行，修复后的 SQL 把每个条件转成 0/1 后求和。

### 6.3 TRAIN_7：Management，修改数据库结构

样本定位：

```text
instance_id: TRAIN_7
db_id: olympics
category: Management
database: database/olympics/olympics_template.sqlite
preprocess_sql: []
clean_up_sql: []
```

模型输入：

```text
query 摘要:
  用户在清理 Olympics 数据库的 person 表结构，原 SQL 删错了列。

issue_sql:
  ALTER TABLE person DROP COLUMN weight
```

参考输出：

```sql
ALTER TABLE person DROP COLUMN height
```

判题逻辑不是查询结果比较，而是直接检查表结构：

```python
info, i_err, i_to = execute_queries(
    ["PRAGMA table_info('person');"],
    db_path, conn, logger=None
)
assert not i_err and not i_to
assert info is not None and len(info) > 0

col_names = [row[1] for row in info]
assert 'height' not in col_names
assert 'weight' in col_names
```

这个例子说明项目不能只支持 SELECT。`Management` 样本会改变数据库状态，所以评测必须使用临时数据库副本，不能直接污染 template database。

### 6.4 TRAIN_8：Management，自定义 test_case 检查约束行为

样本定位：

```text
instance_id: TRAIN_8
db_id: address
category: Management
database: database/address/address_template.sqlite
preprocess_sql: []
clean_up_sql: []
```

模型输入：

```text
query 摘要:
  用户想在 congress 表上创建唯一索引，约束非 NULL 的 district。
  错误点是用 District = NULL，而 SQLite 中判断 NULL 应使用 IS NULL / IS NOT NULL。

issue_sql:
  CREATE UNIQUE INDEX idx_congress_state_district
  ON congress(abbreviation, District)
  WHERE District = NULL;
```

参考输出：

```sql
CREATE UNIQUE INDEX idx_congress_state_district
ON congress(abbreviation, District)
WHERE District IS NOT NULL;
```

关键判题逻辑：

```python
_, exec_error, timeout_error = execute_queries(pred_sqls, db_path, conn)
assert not exec_error
assert not timeout_error

check_index_sql = [
    "SELECT COUNT(*) FROM sqlite_master WHERE type='index' AND name='idx_congress_state_district';"
]
res, err, to = execute_queries(check_index_sql, db_path, conn)
assert res[0][0] == 1

insert_test = [
    "INSERT INTO congress (cognress_rep_id, abbreviation, District) VALUES ('TEST-1', 'ZZ', 1);"
]
_, err1, _ = execute_queries(insert_test, db_path, conn)
assert not err1

insert_duplicate = [
    "INSERT INTO congress (cognress_rep_id, abbreviation, District) VALUES ('TEST-2', 'ZZ', 1);"
]
_, err2, _ = execute_queries(insert_duplicate, db_path, conn)
assert err2
```

这个例子非常像 LeetCode 判题：不是只看 SQL 能不能执行，而是插入测试数据验证唯一索引是否真的生效。

### 6.5 TRAIN_4998：Query，test_case 额外检查具体错误样本

样本定位：

```text
instance_id: TRAIN_4998
db_id: employees
category: Query
database: database/employees/employees_template.sqlite
preprocess_sql: []
clean_up_sql: []
```

模型输入：

```text
query 摘要:
  用户要找 first_name 以 G 开头、last_name 以 F 开头、gender 为 M
  三个条件中恰好满足两个条件的员工。

issue_sql:
  SELECT * FROM employee
  WHERE (first_name LIKE 'G%' AND last_name LIKE 'F%')
     OR (first_name LIKE 'G%' AND gender LIKE 'M%')
     OR (last_name LIKE 'F%' AND gender LIKE 'M%')
  LIMIT 10;
```

参考输出：

```sql
SELECT *
FROM employee
WHERE (first_name LIKE 'G%') + (last_name LIKE 'F%') + (gender LIKE 'M%') = 2
LIMIT 10;
```

关键判题逻辑：

```python
result = ex_base(pred_sqls, sol_sqls, db_path, conn, conditions)
assert result == 1

assert pred_query_result is not None
employee_ids = [row[0] for row in pred_query_result]
assert 10001 not in employee_ids
```

这里 `sol_sql` 要求恰好满足两个条件。员工 `10001` 同时满足三个条件，所以必须被排除。这个例子说明有些 `test_cases` 不只是调用 `ex_base`，还会基于 `pred_query_result` 检查特定反例。

### 6.6 TRAIN_9：Personalization，检查 SQL 结构要求

样本定位：

```text
instance_id: TRAIN_9
db_id: chinook
category: Personalization
database: database/chinook/chinook_template.sqlite
```

模型输入的错误点：

```sql
WHERE p = NULL
```

参考输出的关键修复：

```sql
WHERE p IS NULL
```

关键判题逻辑：

```python
base_result = ex_base(pred_sqls, sol_sqls, db_path, conn, conditions)
assert base_result == 1

required_keywords = ['WITH', 'RECURSIVE', 'UNION']
keyword_result = check_sql_function_usage(pred_sqls, required_keywords)
assert keyword_result == 1

pred_result, pred_exec_error, pred_timeout_error = execute_queries(pred_sqls, db_path, conn)
assert not pred_exec_error
assert not pred_timeout_error
assert pred_result is not None and len(pred_result) > 0

pred_sql_text = ' '.join(pred_sqls).upper()
assert 'IS NULL' in pred_sql_text or 'ISNULL' in pred_sql_text
assert '= NULL' not in pred_sql_text.replace('!= NULL', '').replace('<> NULL', '')
```

这个例子说明 `Personalization` 不只是结果对，还会要求保留某些 SQL 写法或结构，例如必须使用递归 CTE。

## 7. 自动验证机制

自动验证需要同时使用 JSONL 样本和 SQLite 数据库。

完整流程：

```text
1. 读取样本，得到 db_id、query、issue_sql、sol_sql、test_cases。
2. 根据 db_id 找到 database/{db_id}/{db_id}_template.sqlite。
3. 为当前样本创建隔离数据库副本。
4. 执行 preprocess_sql。
5. 模型根据 query + issue_sql + schema 输出 fixed SQL。
6. 执行模型 SQL。
7. 执行 test_cases。
8. test_cases 通过则样本通过；否则失败。
9. 执行 clean_up_sql 或丢弃临时数据库副本。
```

`test_cases` 是 Python 函数字符串。常见形式：

```python
def test_case(pred_sqls, sol_sqls, db_path, conn, conditions):
    result = ex_base(pred_sqls, sol_sqls, db_path, conn, conditions)
    assert result == 1
    return result
```

其中：

- `pred_sqls`：模型输出的 SQL。
- `sol_sqls`：参考 SQL。
- `db_path`：SQLite 数据库路径。
- `conn`：SQLite 连接。
- `conditions`：额外评测条件。

`ex_base` 会执行 `pred_sqls` 和 `sol_sqls`，标准化结果后比较是否一致。

对于 `Management` 类任务，`test_case` 可能不依赖结果集比较，而是检查表结构、数据是否被正确更新、某个触发器或约束是否存在。

## 8. sol_sql 和 test_cases 的关系

`sol_sql` 是参考答案，但最终是否通过主要由 `test_cases` 判定。

常见情况：

- Query 类任务：`test_cases` 调用 `ex_base(pred_sqls, sol_sqls, ...)`，用 `sol_sql` 的执行结果作为 oracle。
- Management 类任务：`test_cases` 直接检查数据库状态，`sol_sql` 更多是参考修复 SQL。
- 如果没有自定义 `test_cases`，可以退化为默认的 pred/sol 执行结果比较。

因此本项目 reward 应以 `test_cases` 为准，而不是做 SQL 字符串匹配。

## 9. 数据划分建议

第一版建议只从 `six-gym-sqlite/train.jsonl` 自己划分：

```text
train: 4,000
val:     500
test:    500
```

划分时尽量保留 `category` 分布，避免验证集全是 Query 或全是 Management。

如果训练时间有限，可以先用小规模子集：

```text
train: 500-1,000
val:   100-200
```

先跑通 baseline evaluation 和 reward，再扩大训练。

## 10. 对工程实现的影响

这份数据决定了项目不应该按 Text-to-SQL 设计，而应该按 SQL debugging/repair 设计：

```text
错误定位：issue_sql 是输入的一部分。
修复目标：输出 fixed SQL。
验证方式：test-case based reward。
数据库需求：必须下载 database/ 下的 SQLite template 文件。
状态隔离：Management 类任务会修改数据库，不能直接污染 template。
```

因此项目实现重点应放在：

- 数据库文件管理。
- schema 生成。
- `preprocess_sql` 和 `clean_up_sql`。
- test case 执行沙箱。
- SQL 执行超时和错误捕获。
- reward 日志和失败样例分析。

这比单纯 Text-to-SQL 更能体现 BIRD-RL 数据定位，也更适合作为后训练/RLVR 简历项目。
