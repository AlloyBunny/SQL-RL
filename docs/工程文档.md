# SQL-RL 工程文档

## 1. 项目结构

计划目录结构：

```text
SQL-RL/
  docs/
    设计文档.md
    数据文档.md
    工程文档.md
    环境配置文档.md
  sql_rl/
    data/
    prompts/
    env/
    rewards/
    eval/
    train/
    utils/
  configs/
  scripts/
  outputs/
  logs/
```

代码目录从第一版开始创建。与 BIRD-RL 行为一致且风险较高的评测逻辑优先以 vendored copy 方式引入，再在 SQL-RL 中写薄适配层。

## 2. 模块划分

`sql_rl/data/`

负责读取 `birdsql/six-gym-sqlite` 和 `birdsql/bird-critic-1.0-sqlite`，生成统一 SQL debugging 样本格式，处理固定 `4000/500/500` train/val/test 划分，以及生成 prompt 所需 schema。第一版训练默认以 `six-gym-sqlite/train.jsonl` 自划分为主，BIRD-Critic-SQLite 合并邮件补丁后作为 external benchmark。

`sql_rl/prompts/`

负责 SQL repair prompt 模板和 chat messages 构造。

`sql_rl/env/`

负责 SQLite 数据库定位、template copy、schema 提取、`preprocess_sql` 执行、SQL 执行、超时控制、结果标准化和数据库状态隔离。

`sql_rl/rewards/`

负责 test-case based correctness reward 和 cost-aware reward。

`sql_rl/eval/`

负责 baseline、SFT、RL 模型的 SQL repair 成功率评估和失败样例导出。

`sql_rl/train/`

负责训练框架适配。第一版优先适配 ms-swift，必要时增加 TRL adapter。

## 3. 数据格式

内部统一样本格式：

```json
{
  "instance_id": "TRAIN_1",
  "db_id": "employees",
  "category": "Query",
  "query": "user issue rewritten in BIRD environment",
  "issue_sql": ["buggy SQL"],
  "schema": "database schema text after optional preprocessing",
  "preprocess_sql": ["SQL statements executed before prediction/evaluation"],
  "clean_up_sql": ["SQL statements used to clean up state"],
  "sol_sql": ["reference repaired SQL"],
  "test_cases": ["python test_case code"],
  "db_path": "data/raw/six-gym-sqlite/database/employees/employees_template.sqlite"
}
```

训练框架需要什么格式，由 adapter 转换，不污染核心数据结构。

需要注意：`six-gym-sqlite` 的 HF repo 带数据库和 `train.jsonl`，但不一定直接带 BIRD-RL 预处理脚本需要的 `after_preprocess_schema`。第一版应从 SQLite 文件和 `preprocess_sql` 自动生成 schema。

## 4. Prompt 格式

第一版使用简单稳定的 SQL debugging prompt：

```text
You are an expert SQL debugger.
Given a user issue, database schema, and a buggy SQL query, return a corrected SQLite SQL query.
Return the final SQL inside <solution>...</solution>.
```

用户消息包含：

```text
## User Issue
{query}

## Database Schema
{schema}

## Buggy SQL
{issue_sql}
```

输出格式：

```text
<solution>
fixed SQL
</solution>
```

第一版优先保证 SQL 抽取稳定。reasoning 可以后续加入，但不应影响 reward 解析。

## 5. SQLite 执行器

执行器需要支持：

- 根据 `db_id` 定位 `database/{db_id}/{db_id}_template.sqlite`。
- 为每个样本创建隔离的临时数据库副本。
- 执行 `preprocess_sql`。
- 执行模型预测 SQL。
- 执行或调用 `test_cases`。
- 在需要时执行 `sol_sql` 作为 oracle。
- 设置超时时间。
- 捕获 SQL 语法错误和执行错误。
- 支持 SELECT 查询和会修改数据库状态的 Management 任务。
- 记录 error type，便于后续分析。

不能只处理 SELECT，因为 BIRD-Critic/SIX-GYM 包含 `Management` 类样本，例如 `ALTER TABLE`、`INSERT`、`UPDATE` 等。

## 6. Reward 模块

基础 reward：

```text
score = 1.0 if all test_cases pass else 0.0
```

部分通过版本：

```text
score = passed_test_cases / total_test_cases
```

cost-aware reward：

```text
score = correctness
        - lambda_len * length_penalty
        - lambda_invalid * invalid_penalty
        - lambda_timeout * timeout_penalty
```

如果后续加入多轮修复：

```text
score = correctness
        - lambda_exec * execution_count_penalty
        - lambda_retry * retry_penalty
```

reward 函数应框架无关。训练框架 adapter 只负责把模型输出和 batch 信息传进来。

## 7. Evaluation 模块

Evaluation 需要输出：

- 总样本数。
- Success Rate / test-case pass rate。
- execution success rate。
- invalid SQL rate。
- timeout rate。
- average SQL length。
- 按 `category` 分组的结果。
- 按是否含 `preprocess_sql` 分组的结果。
- 错误样例 JSONL。

评估结果保存到：

```text
outputs/eval/
```

失败样例至少记录：

```json
{
  "instance_id": "TRAIN_1",
  "db_id": "employees",
  "category": "Query",
  "query": "...",
  "issue_sql": ["..."],
  "pred_sql": ["..."],
  "sol_sql": ["..."],
  "error_type": "assertion_failed | syntax_error | timeout",
  "error_message": "..."
}
```

## 8. 训练后端

第一选择：ms-swift。

理由：

- 使用者更熟悉。
- 对 Qwen 系模型训练更方便。
- 支持 SFT 和 GRPO 类训练流程。
- 自定义 reward 接入成本预计低于 veRL。

备选：TRL。

理由：

- Python 代码透明。
- GRPOTrainer 和 reward function 逻辑容易调试。

暂不使用：veRL。

理由：

- 环境复杂。
- 对第一版单轮 SQL debugging RLVR 不是必要条件。
- BIRD-RL 复杂部分主要集中在 veRL、vLLM、Ray、agent loop 和 patch。

## 9. 模型路线

第一版优先选择 4 * H100 上稳定可跑通 SFT 和 GRPO 的模型，而不是一开始追求最大模型。

推荐顺序：

1. `Qwen2.5-Coder-7B-Instruct`：第一条完整 pipeline 的默认模型，用于基模推理、SFT 和 GRPO。
2. `Qwen2.5-Coder-14B-Instruct`：在 7B 闭环稳定后作为主要增强实验。
3. `Qwen2.5-Coder-32B-Instruct` 或 `Qwen3-14B`：作为更大模型 baseline 或第二阶段训练实验。
4. `Qwen3-Coder-30B-A3B-Instruct`：适合后续推理 baseline 或扩展实验，但不作为第一轮 GRPO 训练目标。

选择原则：

- 第一优先级是完整闭环可复现：数据、prompt、评测、SFT、GRPO、推理结果都能串起来。
- 7B/14B 优先于 32B/MoE，因为 GRPO 阶段比普通 SFT 更容易遇到显存、rollout 和训练稳定性问题。
- 第一版输出格式固定为 `<solution>...</solution>`，避免模型 reasoning 格式影响 reward 解析。

## 10. 开发顺序

第一阶段：文档、项目结构、BIRD-RL critic evaluator vendoring。

第二阶段：下载完整 `birdsql/six-gym-sqlite`，并完成数据完整性检查：

- `train.jsonl` 行数检查。
- `db_id` 覆盖和 SQLite template 文件存在性检查。
- 样本字段检查：`query`、`issue_sql`、`sol_sql`、`test_cases`、`preprocess_sql`。

第三阶段：评测闭环 sanity check：

- synthetic smoke test。
- fake LLM response smoke test。
- `sol_sql` gold-mode 小批量/全量 sanity check。
- `issue_sql` failure sanity check，用于发现 test case 或 evaluator 接入错误。

第四阶段：生成固定 `4000/500/500` split，抽取 schema，构造 prompt 和训练/评测输入文件。

第五阶段：准备外部 SQLite benchmark：

- 下载 `bird-critic-1.0-sqlite` public 数据和 SQLite databases。
- 从 `BIRD-Critic-SQLite-email` 附件提取 `sqlite_sol.jsonl`。
- 按 `instance_id` 合并 public 样本和 `sol_sql/test_cases`。
- 跑 gold sanity check，确认外部 benchmark 与当前 evaluator 兼容。
- 该阶段不要求搭建 BIRD-CRITIC-1 多方言 Docker 环境。

如果第五阶段的数据或合并流程暂时不可用，不阻塞后续训练主线；先用 `six-gym-sqlite` 自建 test split 做主评测。

当前第 1-5 步已完成，关键产物：

```text
data/raw/six-gym-sqlite/
data/processed/six_gym/train.jsonl
data/processed/six_gym/val.jsonl
data/processed/six_gym/test.jsonl
data/raw/bird-critic-1.0-sqlite/
data/processed/bird_critic_sqlite/merged.jsonl
outputs/reports/six_gym_integrity.json
outputs/reports/six_gym_gold_sanity_20.json
outputs/reports/bird_critic_sqlite_integrity.json
outputs/reports/bird_critic_sqlite_gold_sanity_20.json
```

第六阶段：基模推理 baseline，先跑 7B，再根据资源和效果决定是否跑 14B/32B。

第七阶段：SFT，先用 LoRA/QLoRA 跑通，再根据效果决定是否扩大训练规模。

第八阶段：SFT checkpoint 推理和 evaluation，确认 SFT 相比基模有可量化提升。

第九阶段：GRPO / RLVR，先使用 correctness-only reward，再加入 cost-aware reward。

第十阶段：reward ablation、失败样例分析、结果整理和简历表述。

## 11. 最小可行闭环

第一版必须先跑通：

```text
读取 six-gym 样本
-> 数据完整性检查
-> 生成固定 train/val/test split
-> 定位 SQLite template database
-> 构造 SQL debugging prompt
-> 模型生成 fixed SQL
-> 执行 preprocess_sql
-> 执行 fixed SQL
-> 运行 test_cases
-> 计算 reward / evaluation metrics
-> 保存评估结果
```

训练可以晚于 evaluation。只要 evaluation 和 reward 模块先稳定，后续换训练框架的风险会小很多。

在真实模型推理前，必须先完成两类 smoke test：

- fake LLM response：直接把 `sol_sql` 或手写正确 SQL 包进 `<solution>`，验证 evaluator 和 reward。
- gold sanity check：用数据集自带 `sol_sql` 作为预测，验证数据、数据库、`preprocess_sql` 和 `test_cases` 的整体一致性。
