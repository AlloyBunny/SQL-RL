# SQL-RL 执行记录

## 2026-07-01

目标：执行到第 5 步，即完成 `six-gym-sqlite` 下载、uv 最小环境、smoke/gold sanity、固定 split 生成，以及 BIRD-Critic-SQLite 外部 benchmark 的下载、合并和 gold sanity。

当前计划：

1. 下载完整 `birdsql/six-gym-sqlite` 到 `data/raw/six-gym-sqlite`。
2. 创建 uv 最小环境。
3. 运行 synthetic smoke test、fake response sanity、`sol_sql` gold sanity。
4. 生成 `4000/500/500` split 和 prompt-ready JSONL。
5. 下载 `bird-critic-1.0-sqlite` public 数据，合并邮件附件中的 `sqlite_sol.jsonl`，并运行 gold sanity。

执行状态：

- 工具检查：`uv`、Python 3.10、`huggingface-cli` 可用。
- 数据状态：开始前项目内不存在 `data/` 目录。
- 邮件附件状态：`BIRD-Critic-SQLite-email/bird_critic_tc_gt_20260323.zip` 已存在，且目录已加入 `.gitignore`。
- `six-gym-sqlite` 下载完成：`data/raw/six-gym-sqlite`，目录大小约 566M。
- `six-gym-sqlite` 完整性检查通过：`train.jsonl` 5000 行，13 个 `db_id`，13 个 SQLite template 文件齐全，`sol_sql/test_cases` 字段齐全。
- 完整性检查报告：`outputs/reports/six_gym_integrity.json`。
- uv 最小环境已创建：`.venv`，并已完成 `uv pip install -e .`。
- synthetic fake response smoke test 已通过：`scripts/smoke_test_bird_eval.py`，score 1.000。
- `six-gym-sqlite` 前 20 条 `sol_sql` gold sanity 已跑通阈值：19/20，success_rate 0.95，报告 `outputs/reports/six_gym_gold_sanity_20.json`，逐条结果 `outputs/eval/six_gym_gold_sanity_20.jsonl`。
- 已知异常：`TRAIN_8` 为 DDL/index 类 Management 任务，vendored BIRD-RL evaluator 会先执行预测 SQL，再由 test case 再次执行 `pred_sqls`，导致 `CREATE UNIQUE INDEX` 第二次执行失败。当前不修改 vendored evaluator，只在 sanity 命令中显式使用 `--min-success-rate 0.95 --max-failures 1` 让后续准备继续推进。
- `six-gym-sqlite` 固定 split 和 prompt-ready 文件已生成：`data/processed/six_gym/train.jsonl` 4000 行，`val.jsonl` 500 行，`test.jsonl` 500 行，准备报告 `data/processed/six_gym/prepare_report.json`。
- split 分布核对：train 为 Query 2453、Management 878、Personalization 669；val 为 Query 317、Management 106、Personalization 77；test 为 Query 324、Management 103、Personalization 73。三个 split 均覆盖 13 个 `db_id`。
- schema 生成说明：`prepare_report.json` 记录了 187 条 preprocess schema error。脚本已对这些样本回退到原始 template schema 并继续生成 prompt；错误多为 preprocess SQL 本身含约束冲突、重复建表、非 SQLite 方言函数等，不影响当前 split 文件落盘，但后续训练前可再决定是否完全跳过 preprocess schema 或引入更兼容的 schema 生成逻辑。
- `bird-critic-1.0-sqlite` public 数据下载完成：`data/raw/bird-critic-1.0-sqlite`，目录大小约 1.7G，public JSONL 500 行，15 个 `db_id`。
- BIRD-Critic-SQLite 邮件补丁合并完成：`data/processed/bird_critic_sqlite/merged.jsonl` 500 行，`sol_sql/test_cases` 与 public 样本按 `instance_id` 500/500 对齐，无缺失、无额外 solution。合并报告：`outputs/reports/bird_critic_sqlite_merge.json`。
- BIRD-Critic-SQLite 合并集完整性检查通过：500 行，15 个 SQLite template 文件齐全，`query/issue_sql/sol_sql/test_cases` 字段齐全，报告 `outputs/reports/bird_critic_sqlite_integrity.json`。
- BIRD-Critic-SQLite 前 20 条 `sol_sql` gold sanity 通过：20/20，success_rate 1.0，报告 `outputs/reports/bird_critic_sqlite_gold_sanity_20.json`，逐条结果 `outputs/eval/bird_critic_sqlite_gold_sanity_20.jsonl`。

当前结论：第 1-5 步已完成。下一步进入模型侧闭环：先做基模推理 baseline，再做 SFT，最后接 GRPO/RLVR。
